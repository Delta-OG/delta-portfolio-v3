"use client"

import { useState } from "react"

export function BirthdayCelebration() {
  // Disabled by default
  const [isVisible, setIsVisible] = useState(false)

  // No need to show celebration since it's disabled
  return null
}
